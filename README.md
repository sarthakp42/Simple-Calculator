# Simple-Calculator
A simple Java-based calculator that takes two numbers and performs basic arithmetic operations (addition, subtraction, multiplication, division, modulo) based on user input. Includes error handling for division by zero and invalid operation selection.
